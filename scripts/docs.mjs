import {markdown} from './docs/markdown.mjs';
import fs from 'node:fs/promises';
import {operationCatalogue,CONTRACT_VERSION,PROTOCOL_VERSIONS} from '../src/contracts/catalogue.js';
import {RESPONSE_SCHEMAS} from '../src/contracts/responses/index.js';
const escape=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const operations=operationCatalogue().map(({fn,...op})=>({...op,...(RESPONSE_SCHEMAS[op.name]?{responseSchema:RESPONSE_SCHEMAS[op.name]}:{})}));
if(new Set(operations.map(x=>x.name)).size!==operations.length)throw new Error('Duplicate operations');
await fs.mkdir('.site',{recursive:true});
const catalogue={contractVersion:CONTRACT_VERSION,protocolVersions:PROTOCOL_VERSIONS,operations};
await fs.writeFile('.site/contracts.json',JSON.stringify(catalogue,null,2)+'\n');
const paths=Object.fromEntries(operations.map(op=>[`/api/tenants/{workspaceId}/operations/${op.name}`,{post:{operationId:op.name,summary:op.description,security:[{agentKey:[]}],parameters:[{name:'workspaceId',in:'path',required:true,schema:{type:'string'}},{name:'X-VideoAgentVault-Contract',in:'header',schema:{type:'string',const:CONTRACT_VERSION}}],requestBody:{required:true,content:{'application/json':{schema:op.inputSchema}}},responses:{200:{description:'Operation result',...(op.responseSchema?{content:{'application/json':{schema:op.responseSchema}}}:{})},default:{description:'Structured error; do not automatically retry mutations.'}}}}]));
await fs.writeFile('.site/openapi.json',JSON.stringify({openapi:'3.1.0',info:{title:'VideoAgentVault',version:CONTRACT_VERSION},components:{securitySchemes:{agentKey:{type:'http',scheme:'bearer'}}},paths},null,2)+'\n');
const layout=(title,body)=>`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escape(title)} · VideoAgentVault</title><link rel="stylesheet" href="style.css"></head><body><aside><a class="brand" href="index.html">VideoAgentVault</a><p>Developer documentation</p><nav><a href="index.html">Start here</a><a href="installation.html">Cloudflare installation</a><a href="agents.html">Agent workflows</a><a href="reference.html">API & MCP reference</a><a href="cli.html">CLI & MCP clients</a><a href="recovery.html">Database recovery</a><a href="contributing.html">Contributing</a><a href="licensing.html">License & source</a><a href="status.html">Release status</a></nav><small>Contract ${CONTRACT_VERSION}</small></aside><main><header><a href="https://github.com/vidhatanand/VideoAgentVault">Repository ↗</a><span>AGPL-3.0-only</span></header>${body}</main><script src="search.js" defer></script></body></html>`;
const write=(file,title,body)=>fs.writeFile('.site/'+file,layout(title,body));
await write('index.html','Start here',`<p class="eyebrow">VIDEO INFRASTRUCTURE FOR AGENTS</p><h1>Bring video into your agent's world.</h1><p class="lead">Private storage, searchable evidence, derived clips and secure playback in your Cloudflare account.</p><div class="notice">Development preview. Local verification is separate from hosted acceptance. Check the repository's release status before production use.</div><h2>One workspace. Many independent agents.</h2><p>Give each agent a named key, assigned folders, explicit permissions and a spending limit. Shared folders enable collaboration; version checks and claims prevent conflicting edits.</p><ol><li><a href="installation.html">Install in your Cloudflare account.</a></li><li>Sign in through Cloudflare Access and create a folder.</li><li>Create an agent identity, set its budget and save its key securely.</li><li>Upload video and optional captions, then use the same operations through REST or MCP. Build and verify the CLI distribution from source.</li></ol><h2>Capabilities</h2><p>${operations.length} stored-video operations cover upload, indexing, search, evidence, recipes, jobs, clips, playback and agent controls. Video generation, DRM and live monitoring are not advertised.</p><div class="links"><a href="reference.html">Explore the reference →</a><a href="contracts.json">Machine-readable contracts</a><a href="openapi.json">OpenAPI 3.1</a></div>`);
await write('reference.html','API & MCP reference',`<h1>Verified runtime contracts.</h1><p>These ${operations.length} operations are generated directly from the exported runtime. Authentication, folder permissions, budget reservations and input validation apply equally through REST and MCP.</p><label>Find an operation<input id="search" type="search" placeholder="Search uploads, clips, evidence, jobs…"></label><div id="operations">${operations.map(op=>`<article data-search="${escape(op.name+' '+op.description)}"><h2 id="${escape(op.name)}">${escape(op.name)}</h2><p>${escape(op.description)}</p><p><strong>Scope:</strong> <code>${escape(op.scope)}</code> · ${op.annotations.destructiveHint?'Destructive: review exact approval requirements':'Check permissions before invoking'}</p><pre>POST /api/tenants/{workspaceId}/operations/${escape(op.name)}</pre><table><thead><tr><th>Input</th><th>Type</th><th>Required</th></tr></thead><tbody>${Object.entries(op.inputSchema.properties||{}).map(([name,schema])=>`<tr><td><code>${escape(name)}</code></td><td>${escape(Array.isArray(schema.type)?schema.type.join(' | '):schema.type||'see schema')}${schema.enum?'<br>'+escape(schema.enum.join(', ')):''}</td><td>${op.inputSchema.required?.includes(name)?'Yes':'No'}</td></tr>`).join('')}</tbody></table><details><summary>Complete input and output schemas</summary><pre>${escape(JSON.stringify({input:op.inputSchema,output:op.responseSchema||'No strict output schema supplied'},null,2))}</pre></details></article>`).join('')}</div>`);
for(const [file,source,title]of [['cli.html','docs/CLI.md','CLI & MCP clients'],['recovery.html','docs/RECOVERY.md','Database recovery'],['status.html','docs/RELEASE_STATUS.md','Release status'],['security.html','docs/SECURITY.md','Security'],['installation.html','docs/INSTALL_AGENT.md','Cloudflare installation'],['agents.html','docs/AGENT_WORKFLOWS.md','Agent workflows'],['contributing.html','CONTRIBUTING.md','Contributing'],['licensing.html','docs/LICENSING.md','Licensing']])await write(file,title,markdown(await fs.readFile(source,'utf8')));
await fs.writeFile('.site/style.css',`*{box-sizing:border-box}body{margin:0;background:#f6f5f0;color:#18342c;font:16px/1.7 system-ui}aside{position:fixed;inset:0 auto 0 0;width:260px;background:#e8eee5;padding:36px 26px;border-right:1px solid #cbd7cb}.brand{font-weight:800;font-size:21px}a{color:#155e4b}nav{display:grid;gap:16px;margin:36px 0}nav a{text-decoration:none}main{margin-left:260px;padding:32px 7vw 80px;max-width:1400px}header{display:flex;justify-content:space-between;border-bottom:1px solid #ced8cd;padding-bottom:24px;margin-bottom:48px;font-size:13px}h1{font-size:clamp(34px,5vw,62px);line-height:1.1;letter-spacing:-2px;max-width:800px}h2{margin-top:40px}.lead{font-size:22px;max-width:760px}.eyebrow{font-size:12px;letter-spacing:2px}.notice{padding:20px;background:#fff3d4;border-left:3px solid #9a6f12}article{padding:24px;background:white;border:1px solid #d3dcd1;border-radius:10px;margin:24px 0}pre{padding:16px;background:#edf1ea;overflow:auto;font-size:13px;white-space:pre-wrap;overflow-wrap:anywhere}table{width:100%;border-collapse:collapse;font-size:14px}th,td{text-align:left;padding:10px;border-bottom:1px solid #dce3d9}input{display:block;width:100%;padding:14px;font:inherit;border:1px solid #b8c9b7;border-radius:8px}.links{display:flex;gap:24px;flex-wrap:wrap}summary{cursor:pointer}a:focus-visible,input:focus-visible,summary:focus-visible{outline:3px solid #a46b13;outline-offset:3px}[hidden]{display:none!important}@media(max-width:800px){aside{position:static;width:auto;padding:24px}nav{display:flex;gap:16px;flex-wrap:wrap;margin:20px 0}main{margin:0;padding:24px}h1{letter-spacing:-1px}header{margin-bottom:24px}}`);
await fs.writeFile('.site/search.js',"document.querySelector('#search')?.addEventListener('input',event=>{const q=event.target.value.toLowerCase();for(const article of document.querySelectorAll('[data-search]'))article.hidden=!article.dataset.search.toLowerCase().includes(q);});\n");
await fs.mkdir('.site/licenses',{recursive:true});for(const name of await fs.readdir('licenses'))await fs.copyFile('licenses/'+name,'.site/licenses/'+name);
await fs.copyFile('LICENSE','.site/LICENSE.txt');await fs.writeFile('.site/llms.txt','# VideoAgentVault\n\nDevelopment preview.\n\n- [Overview](index.html)\n- [Installation](installation.html)\n- [Contracts](contracts.json)\n- [OpenAPI](openapi.json)\n- [CLI](cli.html)\n- [Recovery](recovery.html)\n- [Security](security.html)\n- [Release status](status.html)\n');await fs.writeFile('.site/.nojekyll','');console.log(`Generated ${operations.length} operations and developer documentation.`);
